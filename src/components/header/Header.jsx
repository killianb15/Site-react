// src/components/header/Header.jsx
import React from 'react';
import './header.css'; // Importer le fichier CSS

const Header = () => {
    return (
        <header className="header">
            <h1>Bouhourd killian</h1>
        </header>
    );
};

export default Header;
